# Installation

We recommend using `uv` for fast, repeatable installs.

```bash
# Install dependencies (dev/test/docs)
uv sync --extra dev --extra test --extra docs
```

To install the library in editable mode:

```bash
uv pip install -e .
```
